import classes from './Subscription.module.css';

const Subscription = () => {
  return (
    <section className={classes['section-sub']}>
      <div>
        <h2>Don't miss out on the monthly newsletters from the community !</h2>
        <br />
        <p>INPUT | SUBSCRIBE</p>
      </div>
    </section>
  );
};

export default Subscription;
